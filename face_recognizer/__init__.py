"""Face recognition access control system.

Sub-packages:
    pipeline    — Detection, liveness, restoration, embedding, matching
    server      — FastAPI HTTP API
    enrollment  — K-Means pose anchor clustering
    models      — Model definitions and weight zoo
    utils       — Training utilities (DDP, logging, checkpointing)
"""
